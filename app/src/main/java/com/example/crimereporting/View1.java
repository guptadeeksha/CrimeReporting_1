package com.example.crimereporting;import androidx.appcompat.app.AppCompatActivity;import android.content.Intent;import android.content.SharedPreferences;import android.database.Cursor;import android.database.sqlite.SQLiteDatabase;import android.os.Bundle;import android.view.View;import android.widget.Button;import android.widget.EditText;import android.widget.ListView;import android.widget.SimpleCursorAdapter;import android.widget.TextView;import android.widget.Toast;import android.app.ListActivity;import java.util.List;public class View1 extends AppCompatActivity {    TextView t1;    public static String SearchQuery = "SELECT ";    ComplainDatabasehelper ds;    EditText e1;    String s1,z;        SimpleCursorAdapter adap;    Cursor cu;    ListView lv;    public void call(SQLiteDatabase sq)    {    }    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.view);        t1=findViewById(R.id.View1);        Button Search = findViewById(R.id.btnsearch);        //e1=findViewById(R.id.search);        //ds=new ComplainDatabasehelper(this);        //final Cursor cursor= ds.ViewData();        Search.setOnClickListener(new View.OnClickListener() {            @Override                public void onClick(View view) {                    //SharedPreferences.Editor edit = getSharedPreferences("personal", MODE_PRIVATE).edit();//                s1 = e1.getText().toString();//                z=ds.check(s1);//                if (s1.equals(z)) {//////                }//                else//                {//                    Toast.makeText(getApplicationContext(), "User data unavailable", Toast.LENGTH_LONG).show();//                }             }        });    }    public void View1(View view) {    }    private View.OnClickListener fnsearch= new View.OnClickListener() {        @Override        public void onClick(View view) {            Intent intent1;            intent1 =new Intent(View1.this, View1.class);            startActivity(intent1);        }    };}